# Optional: add local-only env vars here and copy to .env.local if needed.
# This app does not require API keys for the default (offline) experience.

# Deploy under a subpath (e.g. https://neurosymphony.ai/projects/javaviz/):
#   JAVAVIZ_BASE=/projects/javaviz/ npm run build
# Or: npm run build:subpath
# Preview local build with that base: npm run preview:subpath
